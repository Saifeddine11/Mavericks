# Content — forms & qualification

| Concern | Location |
|---------|----------|
| Enquiry copy | `src/i18n/locales/fr.json` → `enquiry.*` |
| Field labels | `enquiry.fields.*`, `propertyTypes`, `objectives` |

Form UI components (when wired): `src/sections/EnquireCard.jsx` and future `src/components/forms/`.

Qualification fields direction: type de bien, objectif, budget, zone, délai, critères.
